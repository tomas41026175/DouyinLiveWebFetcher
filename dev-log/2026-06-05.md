# 2026-06-05

## chatroom 中國訪問問題 → 全上雲（branch: fix/china-access）

### 問題診斷（systematic-debugging）
- 症狀：trycloudflare 網址中國端「无法访问/检查拼写」= DNS NXDOMAIN
- 採證：台灣 `curl` 該網址 HTTP 200、中國端解析失敗 → **GFW 對 `trycloudflare.com` DNS 污染**（間歇性）
- 鐵證：`git diff cc11e42 00b13cc -- start_all.sh` 隧道建立/抓網址邏輯（line 102/106）逐字相同 → **非本次 merge 造成**

### 決策
- 先 rollback main（revert 3 個 commit），在 `fix/china-access` 處理（從 00b13cc 重建 + revert-the-reverts 恢復功能，避免 revert 陷阱）
- 方案評估：trycloudflare/ngrok（中國封）、cpolar/natapp（要大陸實名，台灣人辦不了）、Cloudflare named tunnel（IP 仍被封）
- 收斂：免實名 + 可付費 → 海外 VPS → 最終「**整份上雲**」（含 web_danmaku）

### 全上雲（Vultr 東京 1C/2GB Ubuntu 24.04）
- chatroom systemd（:80 公網，中國直連 VPS IP）+ web_danmaku systemd（抓抖音）
- 雙向鏡像（同機 localhost）；兩服務 enabled 開機自動起
- 安全加固：SSH key 登入 + 換掉對話中暴露的 root 密碼
- **抖音抓取從日本 VPS 實測成功**（WebSocket 連上、未被風控）→ Plan B 不需要

### 新功能（這次加的）
- CRUD 貼圖：`/admin` 管理頁 + 密碼，base64 JSON 上傳（免 multer 依賴），中文檔名 OK
- 改暱稱：房內「改暱稱」鈕 + 首次掃碼進房（訪客）自動提示 → 解決直接進房沒暱稱問題
- cache 對話：記憶體 + UTC+8 每日重置，重整/重連載入當天歷史（history 在 room_created/joined 之前送、重連時 DOM 已有則跳過避免重複）

### 關鍵踩坑
- `mini-racer`(PyPI) import 名是 `py_mini_racer`（diag 一度測錯 module 名）
- web_danmaku.py 依賴 `liveMan.py`（簽名核心）— 第一次打包漏了
- Ubuntu 24.04 預設無 `python3.12-venv`
- python stdout pipe 緩衝 → 診斷要 `python -u`
- systemd 用 sftp 寫 service 檔沒 close → Environment 沒 flush → 鏡像 env 失效（debug 一輪）
- 房號必須 6 位英數 → 測試用 5 字元（MIRR3）會誤判「鏡像失敗」

### 收尾
- 還原 start_all.sh 成本機開發版（移除 frp）、刪 frp.conf.example/setup-frps.sh
- 新增 deploy/cloud-deploy.md 部署文件

## 版本號改用 package.json version（PR #8）
- 問題：admin 顯示 git sha（`7297ff5`）像分支/代號，非真正版本號
- 改動：`/version` 改讀 `package.json` 的 `version` → 回 `{ version, startedAt }`；admin 顯示 `v1.0.0 · 部署時間`
- 清理：deploy.yml 不再寫 `.build`、.gitignore 移除 `.build`（rsync --delete 會自動清掉 VPS 上殘留）
- 驗證：Actions success → VPS `/version` 回 `{"version":"1.0.0",...}` ✓
- 之後要標版本：編輯 `chatroom/package.json` 的 `version` 即可
