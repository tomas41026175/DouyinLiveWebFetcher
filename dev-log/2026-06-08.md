# 2026-06-08

## 整理 branch + 恢復 start_all.sh trycloudflare 隧道
- 整理 branch：遠端 16 個 stale ref 經 `fetch --prune` 清除，GitHub + 本地遠端只剩 main
- 恢復隧道：從舊 commit `5708d21` 取回邏輯整合進現在 start_all.sh（保留自動更新 / 開分頁 / ROOM 等改進）
  - [1/5] 加 cloudflared 檢測+安裝、`CF_LOG` 變數、[4/5]+cleanup 清 CF_LOG、[5/5] `cloudflared tunnel` + 抓 trycloudflare 網址
- 一致性發現：`start_all.bat`(Windows) 從沒被改成本機模式、一直是隧道版（[5/5] 完整）→ 恢復 .sh 後兩邊一致
- 註：trycloudflare GFW 間歇封鎖；start_all.sh 不部署 VPS（VPS 用 systemd），只影響本機 git clone

## 先前（web_danmaku 資料來源切換）
- web_danmaku :8765 改 `DY_BIND` env 控制可對外（VPS systemd drop-in + ufw 8765），webUI 加「⇄ 另一版」切換 link（本機 ⇄ VPS）。PR #18 merged。
