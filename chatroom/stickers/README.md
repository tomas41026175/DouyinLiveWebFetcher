# 貼圖資料夾

把要在聊天室使用的貼圖**圖片檔**放進這個資料夾即可，房間內所有人都能使用。

- 支援格式：`.png` `.jpg` `.jpeg` `.gif`（含動圖）`.webp`
- 建議單張 ≤ 2MB（太大會拖慢載入）
- 圖片檔**不會進 git**（屬於你本機的內容），只有這個說明與資料夾本身保留

## 整理檔名（選用）

檔名雜亂時，可在 `chatroom/` 目錄執行：

```bash
npm run stickers:rename
```

會依「加入時間」把檔名整理成 `001.png`、`002.gif`…，讓貼圖面板顯示順序固定。
